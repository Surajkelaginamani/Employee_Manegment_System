name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Verify build artifacts
        run: |
          echo "Listing dist contents"
          ls -la ./dist
          echo "Size summary"
          du -sh ./dist || true

      - name: Upload artifact for GitHub Pages
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./dist

  deploy:
    needs: build-and-deploy
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1
        with:
          # uses the workflow permissions to deploy
          # no extra inputs required
          
      - name: Enable GitHub Pages
        uses: actions/github-script@v6
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const octokit = github.getOctokit(process.env.GITHUB_TOKEN);
            // Configure Pages to serve from the gh-pages branch or the deployment
            // The Pages deployment performed by actions/deploy-pages will handle activation,
            // this step is a no-op in most cases but kept for clarity.
            return 'Pages deployment completed.'
